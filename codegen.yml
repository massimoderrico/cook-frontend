overwrite: true
schema: 'http://localhost:4000/graphql' # Fetch schema from backend
generates:
  types/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
